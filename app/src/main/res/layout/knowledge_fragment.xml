<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:orientation="vertical"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@android:color/transparent">

    <include layout="@layout/knowledge_panel"
        android:id="@+id/knowledgeFormPanel"/>

    <ListView
        android:animateLayoutChanges="true"
        android:id="@+id/knowledgeUserListView"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        style="@style/list_default_style"
        android:background="@android:color/transparent"
        android:layout_below="@+id/knowledgeFormPanel"
        android:layout_alignParentBottom="true" />

    <android.support.design.widget.FloatingActionButton
        android:id="@+id/addKnowledgeButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:src="@drawable/btn_plus"
        app:backgroundTint="@color/font_reverse"
        app:elevation="6dp"
        app:fabSize="normal"
        app:pressedTranslationZ="12dp"
        app:borderWidth="0dp"
        android:layout_alignBottom="@+id/knowledgeFormPanel"
        android:layout_alignParentRight="true"
        android:layout_alignParentEnd="true"
        android:layout_marginBottom="-25dp" />

</RelativeLayout>